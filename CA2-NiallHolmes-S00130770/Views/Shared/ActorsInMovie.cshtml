@model IEnumerable<CA2_NiallHolmes_S00130770.Models.Actor>


@if(Model.Any())
{
    
<div class="container">
    <div class="panel panel-success"; style="margin-right:40px">
        <div class="panel-heading">
    <h2>Add,Edit and Delete Actors here</h2>
    </div>
<table class="table table-condensed table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ActorScreenName)
        </th>
        <th>
        </th>
    </tr>

    @if(Model != null)
    {
    foreach (var item in Model) 
        {
        <tr>
            <td>
            @Html.DisplayFor(modelItem => item.ActorName)
            </td>
            <td>
            @Html.DisplayFor(modelItem => item.ActorScreenName)
            </td>
            <td>
            @Html.ActionLink("Edit", "EditActor", new { id=item.ActorID }, new { @class = "btn btn-info btn-sm" }) 
            @Html.ActionLink("Delete", "DeleteActors", new { id=item.ActorID }, new { @class = "btn btn-danger btn-sm" })
            </td>
        </tr>
        }
    }
    
</table>
     <div class="panel-footer">
   <a class="btn-sm btn-success" href="@Url.Action("CreateActor")"><span class="glyphicon glyphicon-plus"></span>&nbsp;Add Actor</a>
   <a class="btn-sm btn-info" href="@Url.Action("Index")"><span class="glyphicon glyphicon-remove"></span>&nbsp;Close</a>
    </div>  
       

    </div>
</div>
}
 else
    {<div class="panel panel-success">
        <div class="panel-heading">
    <h2>Add,Edit and Delete Actors here</h2>
    </div>
        <div style="margin-left:15px">
        <strong><mark>No Actors</mark></strong>
            </div>
        <div class="panel-footer">
   <a class="btn-sm btn-success" href="@Url.Action("CreateActor")"><span class="glyphicon glyphicon-plus"></span>&nbsp;Add Actor</a>
   <a class="btn-sm btn-info" href="@Url.Action("Index")"><span class="glyphicon glyphicon-remove"></span>&nbsp;Close</a>
    </div>  
     </div>}